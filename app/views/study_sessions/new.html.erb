<h1>New study_session</h1>

<%= form_for @study_session, url: room_study_sessions_path do |f| %>
  <% if @study_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@study_session.errors.count, "error") %> prohibited this study_session from being saved:</h2>

      <ul>
      <% @study_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :name %><br/>
  <%= f.text_field :name %><br/>

  <%= f.label :summary %><br/>
  <%= f.text_field :summary %><br/>

  <%= f.submit %>

<% end %>

<%= link_to 'Back', room_study_sessions_path %>

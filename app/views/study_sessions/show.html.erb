<p id="notice"><%= notice %></p>

<h3>Study Session: <%= @study_session.name %></h3>
<p>In room <%= link_to @study_session.room.room_number, @study_session.room %> in <%= link_to @study_session.room.building.name, @study_session.room.building %> at <%= link_to @study_session.room.building.school.name, @study_session.room.building.school %></p>

<p>
  Summary of this study session:
  <br/>
  <%= @study_session.summary %>
</p>

<p><%= link_to 'Add a Note to This Study Session', new_room_study_session_note_path(@study_session.room, @study_session) %></p>

<table class="table table-hover table-responsive">

<thead>
  <tr>
	<th>Note</th>
	<th>Edit</th>
	<th>Destroy</th>
  </tr>
</thead>

<tbody>
  <% @study_session.notes.each do |note| %>
	<tr>
	  <td><%= note.summary %></td>
	  <td><%= link_to 'Edit', edit_room_study_session_note_path(note.study_session.room, note.study_session, note) %></td>
	  <td><%= link_to 'Destroy',  room_study_session_note_path(note.study_session.room, note.study_session, note), method: :delete, data: { confirm: 'Are you sure?' } %></td>
	</tr>
  <% end %>
</tbody>
</table>

<br/>

<%= link_to 'Edit Study Session', edit_room_study_session_path(@study_session.room, @study_session) %> |
<%= link_to 'Back To ' + @study_session.room.building.name + " " + @study_session.room.room_number, room_path(@study_session.room) %>
